import Reports from "@/components/reports/Reports";
import React from "react";

export const metadata = {
  title: "Reports Module | CTRL-Room",
  description: "Gain full visibility into your workforce, site activities, shift status, and daily operations with Ctrl Room's comprehensive Reports Module.",
};

const page = () => {
  return <Reports />;
};

export default page;
